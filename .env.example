# Google Gemini API Key for AI Chatbot (server-side only)
GEMINI_API_KEY=

# Vercel Postgres (Serverless PostgreSQL - automatically injected when connecting Vercel Postgres Storage)
POSTGRES_URL=
DATABASE_URL=

# Vercel KV REST API (Serverless Redis - automatically injected when connecting Vercel KV Storage)
KV_REST_API_URL=
KV_REST_API_TOKEN=
